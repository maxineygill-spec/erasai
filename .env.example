# Eras AI — copy to .env and fill in values. Never commit .env.

# Backend (server) — used by Express
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Frontend (Vite) — prefix with VITE_ to expose to client
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
